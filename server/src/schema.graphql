schema {
    query: Query
    mutation: Mutation
}

type Query {
    users: [User!]!
    user(id: ID!): User
    # locationInRange(l1: Location, l2: Location): Boolean
}

type Mutation {
    createUser(name: String!, email: String!): User!
    deleteUser(id: ID!): User
    updateLastKnown(id: ID!, latitude: String!, longitude: String!): User
}

type User {
    id: ID!
    name: String!
    email: String!
    objectiu: User
    latitude: String
    longitude: String
}

input UserUpdateInput {
    latitude: String
    longitude: String
}