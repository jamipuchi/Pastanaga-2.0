type User {
    id: ID! @id
    createdAt: DateTime! @createdAt
    name: String!
    email: String! @unique
    objectiu: User @relation(name: "Objectiu" onDelete: SET_NULL link: INLINE)
    latitude: String
    longitude: String
    alive: Boolean
    monedes: Int
    winner: Boolean!
}
